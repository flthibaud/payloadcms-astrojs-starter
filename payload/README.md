# Payload + Next starter

This project boots a Payload 3 app mounted in Next.js and backed by PostgreSQL. Copy
`.env.example` to `.env` (the defaults line up with `docker-compose.yml`) and run
`pnpm dev` or the workspace root script.

## Attributes

- **Database**: PostgreSQL
- **Storage Adapter**: localDisk
